<?xml version="1.0" encoding="utf-8"?>
<!--
  Deliberately not restricted to a specific packageNames list: the native
  USSD reply dialog's owning package varies by OEM (stock AOSP is
  com.android.phone; Samsung/Xiaomi/Huawei/etc ship their own), and this
  needs to work across the actual field devices without a hardcoded,
  possibly-incomplete allowlist. Real acting on what it reads is still
  gated in code by ExchangeUssdBridge.armed — see
  ExchangeUssdAccessibilityService.kt.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagRetrieveInteractiveWindows|flagReportViewIds"
    android:canRetrieveWindowContent="true"
    android:canPerformGestures="false"
    android:description="@string/exchange_accessibility_description"
    android:notificationTimeout="100" />
