<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Only used to auto-detect the OTP code while the verification-code screen
         is open (see sms/OtpSmsListener.kt) — registered dynamically for that
         window only, never a manifest-declared always-on receiver. -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <!-- Only used to detect and label a device's active SIMs (e.g. "SIM 1:
         Hormuud Telecom") in the payment bottom sheet's dual-SIM picker
         before dialing via ACTION_DIAL — requested at runtime only when that
         dialog would actually be shown; never used to place calls directly. -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:name=".DalabCustomerApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.DalabCustomerApp"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.DalabCustomerApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
